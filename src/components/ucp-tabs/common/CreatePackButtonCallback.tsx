import { useExtensionState } from 'hooks/jotai/globals-wrapper';

const CreatePackButtonCallback = () => {
  const { activeExtensions } = useExtensionState();
};

export default CreatePackButtonCallback;
