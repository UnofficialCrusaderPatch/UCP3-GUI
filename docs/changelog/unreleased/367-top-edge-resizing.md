# Next GUI release — top-edge window resizing

- Restore resizing from the top edge and upper corners of the GUI window by keeping the resize area separate from titlebar dragging. The titlebar below it still moves the window; maximized windows keep their existing behavior.

PR: https://github.com/UnofficialCrusaderPatch/UCP3-GUI/pull/367

Release preparation: include these notes in the GUI wiki's next-release changelog once this PR is merged. The currently planned next GUI release is v1.0.16. This file does not change the GUI version or publish a release.
